specVersion: "v1beta"
serviceId: "decision-tree"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "decision-tree-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
